# ======================================================
# 🔹 YOUTUBE API PARAMETERS
# ======================================================

# Clé d’API YouTube pour accéder à l’API Data v3 de Google.
# ⚠️ À garder strictement privée ! Elle permet d’interroger les données (chaînes, vidéos, etc.)
API_KEY="AIzaSyD_MTThH71e6InXgwDTzAOh7aoQ_4"

# Nom d’utilisateur (handle) de la chaîne YouTube à suivre.
# Exemple : "MrBeast" → sera utilisé pour extraire les vidéos, statistiques, etc.
CHANNEL_HANDLE="MrBeast"


# ======================================================
# 🔹 DOCKERHUB IMAGE CONFIGURATION
# ======================================================

# Namespace (ou nom d’utilisateur) Docker Hub — correspond à ton compte Docker Hub.
DOCKERHUB_NAMESPACE=mareldataengineer

# Nom du dépôt d’image Docker sur Docker Hub.
DOCKERHUB_REPOSITORY=yt_api_elt

# Tag (version) spécifique de ton image Docker.
# Permet de versionner ton image (par ex. 1.0.0, latest, etc.)
IMAGE_TAG=1.0.0


# ======================================================
# 🔹 POSTGRESQL CONNECTION (Airflow & ELT)
# ======================================================

# Nom d’utilisateur PostgreSQL pour se connecter à la base.
# Ici "postgres" est l’utilisateur par défaut, souvent déjà créé automatiquement.
POSTGRES_CONN_USERNAME=postgres

# Mot de passe de cet utilisateur PostgreSQL.
POSTGRES_CONN_PASSWORD="wWfKvYbyD3kRnE4u"

# Nom du service ou conteneur Docker qui héberge PostgreSQL.
# Dans un docker-compose, il correspond au nom du service, ex: "postgres".
POSTGRES_CONN_HOST=postgres

# Port d’écoute de PostgreSQL (5432 est le port standard).
POSTGRES_CONN_PORT=5432


# ======================================================
# 🔹 AIRFLOW METADATA DATABASE (interne à Airflow)
# ======================================================

# Nom de la base de données utilisée par Airflow pour stocker sa configuration et ses DAGs.
METADATA_DATABASE_NAME=airflow_metadata_db

# Nom d’utilisateur de la base de métadonnées Airflow.
METADATA_DATABASE_USERNAME=airflow_meta_user

# Mot de passe associé à cet utilisateur.
METADATA_DATABASE_PASSWORD="VNXkgKEPBn69yYwA"


# ======================================================
# 🔹 CELERY BACKEND (pour exécuter des tâches en parallèle)
# ======================================================

# Nom de la base de données utilisée pour stocker les résultats des tâches Celery.
CELERY_BACKEND_NAME=celery_results_db

# Identifiants de connexion à cette base (utilisateur + mot de passe).
CELERY_BACKEND_USERNAME=celery_user
CELERY_BACKEND_PASSWORD="L4PYpRNq6mxSQfyj"


# ======================================================
# 🔹 ELT (Extract Load Transform) DATABASE
# ======================================================

# Base de données qui contient les données extraites (YouTube) et transformées.
ELT_DATABASE_NAME=elt_db

# Identifiants pour s’y connecter (utilisateur + mot de passe).
ELT_DATABASE_USERNAME=yt_api_user
ELT_DATABASE_PASSWORD="X57tmQ846GYP3Jgb"


# ======================================================
# 🔹 AIRFLOW PARAMETERS
# ======================================================

# UID (identifiant utilisateur) utilisé par Airflow dans le conteneur Docker.
# 50000 est la valeur par défaut recommandée par Apache Airflow.
AIRFLOW_UID="50000"

# Identifiants pour accéder à l’interface web d’Airflow.
AIRFLOW_WWW_USER_USERNAME="airflow"
AIRFLOW_WWW_USER_PASSWORD="airflow1234"

# Clé Fernet utilisée pour chiffrer les mots de passe et connexions stockés par Airflow.
# ⚠️ À garder absolument secrète : si elle change, toutes les connexions Airflow chiffrées deviennent invalides.
FERNET_KEY="pKInkePlkl79C3mafrjthiQdXnsDU8EXjWDZi-SU00U="
